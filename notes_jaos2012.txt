-*- mode:outline;coding:utf-8 -*-
* sara
bundle (envoyer les buffer exactement en même temps)
s.makeBundle( 1, { Synth( "default" ); Synth( "default", [ \freq, 660 ] ) } ); 

* pampos
jouer avec la voix (omniprésente dans les quatre du haut)

* flavie
server.doWhenBooted

** fait
normalisations noms de fichiers
passer en mono
